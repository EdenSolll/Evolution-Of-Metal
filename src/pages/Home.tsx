// Import necessary dependencies and components
import LeafletMap from "../components/map.tsx";

const App: React.FC = () => {
  return (
    <div>
        <LeafletMap/>
    </div>
  );
};

export default App;
